{
  "name": "imgbest-ai-product-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node --no-warnings server.js",
    "check": "node --check app.js && node --check server.js && node --check src/db.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "dotenv": "^16.5.0",
    "express": "^5.2.1",
    "nodemailer": "^8.0.10",
    "prisma": "^6.9.0"
  }
}
